mixin thumbnail-classic(obj)
  // Thumbnail Classic
  article.thumbnail.thumbnail-classic(class=obj.mod)&attributes(attributes)
    a.thumbnail-classic-figure(href='images/' + obj.imgOriginal + '.jpg' data-lightgallery='item')
      +jpg(obj.img)

    .thumbnail-classic-caption
      h6.thumbnail-classic-title: a(href='project-page.html')=obj.title
      .thumbnail-classic-time
        if(obj.time)
          time(datetime=obj.time.datetime)=obj.time.dateText
        else
          time(datetime='2021-04-05') April 05, 2021

mixin thumbnail-modern(obj)
  // Thumbnail Modern
  article.thumbnail.thumbnail-modern(class=obj.mod)&attributes(attributes)
    a.thumbnail-modern-figure(href='images/' + obj.imgOriginal + '-1200x800-original.jpg' data-lightgallery='item')
      +jpg(obj.img)

    .thumbnail-modern-caption
      h5.thumbnail-modern-title: a(href='project-page.html')=obj.nameProject ? obj.nameProject : 'Project name #' + (obj.numberProject ? obj.numberProject : obj.numberImg)
      

mixin thumbnail-creative(obj)
  // Thumbnail Creative
  article.thumbnail.thumbnail-creative(class=obj.mod)&attributes(attributes)
    a(href='images/' + obj.imgOriginal + '.jpg' data-lightgallery='item')
      .thumbnail-creative-figure: +jpg(obj.img)
      .thumbnail-creative-caption: span.icon.thumbnail-creative-icon.linearicons-magnifier

mixin thumbnail-mary(obj)
  // Thumbnail Mary
  article.thumbnail.thumbnail-mary(class=obj.mod)&attributes(attributes)
    a.thumbnail-mary-figure(href='images/' + (obj.nameImgOriginal ? obj.nameImgOriginal : obj.nameImg + '-' + (obj.numberImgOriginal ? obj.numberImgOriginal : obj.numberImg)) + '-1200x800-original.jpg' data-lightgallery='item'): +jpg(obj.nameImg + '-' + obj.numberImg + '-' + obj.sizeImg)
    .thumbnail-mary-caption: div
      h6.thumbnail-mary-title: a(href='project-page.html')=obj.title
      .thumbnail-mary-location=obj.location